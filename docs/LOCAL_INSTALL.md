# Local Install

Use this repo as a local build.

```bash
./build.sh
```

Then open the generated installer image in `dist/`, drag `ClaudeMonitor.app` into `Applications`, and launch it.

For the one-shot Claude Code prompt, see the root `README.md`.
